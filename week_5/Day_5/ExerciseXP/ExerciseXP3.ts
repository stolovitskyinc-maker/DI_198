// Define a variable of type 'any'
let someValue: any = "Hello, TypeScript!";

// Cast using the 'as' syntax
let stringValue = someValue as string;

// Use it as a string
console.log(stringValue.toUpperCase()); 
